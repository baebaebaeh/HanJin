package net.acmicpc.num2166;

import java.io.*;
import java.util.*;

public class num2166 {
	public static void main(String[] args) throws IOException {
		System.setIn(new FileInputStream("data/input2166.txt"));
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
		int N = Integer.parseInt(br.readLine());
		StringTokenizer st = new StringTokenizer(br.readLine());
		int stanX = Integer.parseInt(st.nextToken());
		int stanY = Integer.parseInt(st.nextToken());
		st = new StringTokenizer(br.readLine());
		int varX = Integer.parseInt(st.nextToken());
		int varY = Integer.parseInt(st.nextToken());
		for (int i = 0; i < N - 2; i++) {
			
		}
	}
}